# Changelog

本文件记录 Switch Tracker 的重要变化。项目从现在开始采用 [Keep a Changelog](https://keepachangelog.com/en/1.1.0/) 的结构，并以语义化版本作为发布参考。

## [Unreleased]

项目正在进行第一次按可维护开源项目标准的系统整理；以下内容会随下一次正式版本一起发布。

### Added

- 集中的 SQLite schema、迁移和持久化模块
- 数据库、API 和敏感文件暴露回归测试
- GitHub Actions CI、Dependabot 和开发依赖配置
- 本地占位图标、健康检查接口和安全响应头
- 贡献指南、安全政策、行为准则、路线图和架构文档

### Changed

- 每日记录改为幂等更新，重复采集不会重复累计
- 游戏信息更新时保留已有中文名称
- Web 服务默认只监听 `127.0.0.1`，debug 默认关闭
- 原始 API JSON 改为通过 `--archive-json` 显式保存
- 定时采集脚本不再管理或重启 Web 进程
- 前端日期按本地日历日期解析，避免负时区显示前一天

### Security

- 不再把整个仓库目录作为静态文件提供，避免数据库、令牌和 `.git` 被读取
- 令牌文件采用原子写入并设置为仅当前用户可读写
- 动态游戏名称和图片 URL 在渲染前进行转义或校验

### Fixed

- 采集遇到保存失败、无效 JSON 或响应结构异常时返回失败退出码
- 搜索、清空搜索、排序及刷新保留完整游戏列表与筛选状态
- 空数据库总览显示首次采集提示，不再抛出空数组异常
- 日历切换月份固定在月初，避免月底跳过短月份
- 每日快照允许修正为零分钟，拒绝旧快照覆盖新记录
- 清空 CSV 中文名后导入可移除翻译，后续采集不会恢复旧翻译
- 新增采集、前端交互、快照边界和翻译流程回归测试
